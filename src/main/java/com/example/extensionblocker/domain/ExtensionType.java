package com.example.extensionblocker.domain;

/**
 * FIXED  — one of the 7 predefined extensions, always present, toggled on/off.
 * CUSTOM — user-added extension, present only while blocked.
 */
public enum ExtensionType {
    FIXED,
    CUSTOM
}
